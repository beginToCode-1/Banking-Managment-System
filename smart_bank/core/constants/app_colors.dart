import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const Color primary = Color(0xFF1A3C6E);       // Deep bank blue
  static const Color primaryLight = Color(0xFF2A5298);
  static const Color primaryDark = Color(0xFF0D1F3C);

  // Accent
  static const Color accent = Color(0xFFD4A017);        // Gold accent

  // Background
  static const Color background = Color(0xFFF5F7FA);
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textLight = Color(0xFFFFFFFF);

  // Status
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color pending = Color(0xFF3B82F6);

  // Transaction Types
  static const Color deposit = Color(0xFF10B981);
  static const Color withdrawal = Color(0xFFEF4444);
  static const Color transfer = Color(0xFF3B82F6);

  // Divider & Border
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFF3F4F6);
}